<%= render template: 'navbar/index' %>
<p>
  <strong>Title:</strong>
  <%= @list.title %>
</p>

<p>
  <%= @list.body %>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
